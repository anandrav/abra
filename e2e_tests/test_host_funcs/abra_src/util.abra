// TODO these tests really need to be more thorough. test more kinds of types, including void in enums and structs.

use util_helper
use more_stuff/derp

#host
fn foo(a: int, b: string) -> int

#host
fn bar(a: int, b: int) -> (int, int)

#host
type Color =
    | Red
    | Blue
    | Green

#host
fn make_person(name: string, age: int) -> Person

#host
fn get_blah() -> Blah