use util

let (x, y) = bar(2, 3)

// store host function in variable/use as lambda
let b = bar
let (u, v) = b(4, 5)

// test host types: Person and Blah
let p = make_person("Alice", 30)
let bl = get_blah()

x + y + u + v + p.age + bl.n