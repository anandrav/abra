[package]
name = "test_ffi"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
abra_core = { path = "../../../../../core" }

[build-dependencies]
abra_core = { path = "../../../../../core" }
