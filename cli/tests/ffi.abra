use test_ffi

println(pass_int(35))
println(pass_bool(false))
println(pass_void(()))
println(pass_string("mystring"))
let s = pass_struct(MyStruct(23, true, (), "another"))
println(s.i)
println(s.b)
println(s.v)
println(s.s)
let e = pass_enum(String("yet another"))
match e {
    String(~s) -> println(s)
    _ -> println("<wrong variant>")
}
let t = pass_tuple((true, 2, "3"))
println(t)
let m = pass_maybe(yes("yes"))
match m {
    yes(~s) -> println(s)
    _ -> println("wrong variant")
}
