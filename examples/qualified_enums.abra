type Color = 
  | Red
  | Blue
  | Green

implement ToString for Color {
  fn str(c) = match c {
    | Color.Red -> "red" // the Color. prefix is unused right now
    | .Blue -> "blue"
    | .Green -> "green"
  }
}

let c = Color.Red // this only works for variants with no data
println(c)

