use time
use os
use os/exec
use os/env

// let path = "examples/use_os.abra"
//let contents = fread(path)
//match contents {
//  yes(~c) -> print(c)
//  no(_) -> {}
//}
//let contents = unwrap(contents)

// command("/bin/echo echooooo")

let time = get_time()
println("seconds: " & time)

let home = get_var("HOME")
println("home dir is: " & home)

println("time to take a 1 second nap")

sleep(1.0)

println("ok done!")

