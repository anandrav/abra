type Person = {
	first_name: string
	last_name: string
	age: int
}

extend Person {
	fn fullname(self) -> string {
		self.first_name & " " & self.last_name
	}
}

let p: Person = Person("Anand", "Dukkipati", 26)
println(p.fullname())
