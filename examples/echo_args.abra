let args = get_args()
let n = len(args)

if n = 0 {
  println("You didn't pass any args. Try doing `abra echo_args.abra <arg1> <arg2>`")
} else {
  println("You gave these args:")
}

var i = 0
while i < n {
  println(i+1 & ". " & args[i])
  i := i + 1
}
