# Development

```
./scripts/setup              # Install dev dependencies (wasm target, cargo-deny, mdbook, etc.)
./scripts/test               # Run full test suite
./scripts/run <file>.abra    # Run Abra using local modules/ (instead of system-installed modules)
```
